# catalog/redis.yml
image_repo: "redis"
restart_policy: "always"

# Agnostic Storage Definition
volumes:
  redis-data:
    target: "/data"

healthcheck:
  test: ["CMD", "redis-cli", "ping"]
  interval: "30s"
  timeout: "5s"
  retries: 5