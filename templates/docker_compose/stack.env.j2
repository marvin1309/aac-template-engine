{#
# This template generates the stack.env file.
# It iterates through the 'stack_env' dictionary provided by the
# context ('data' in the Python script), which comes from either
# service.yml (in CI) or the merged final_ssot_vars (in Ansible).
#}
{% for key, value in deployments.docker_compose.stack_env.items() %}
{{ key }}="{{ value }}"
{% endfor %}